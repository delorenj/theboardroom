# theboardroom Environment Configuration

# Bloodbank WebSocket URL (STOMP over WebSocket)
# Set this to connect to a live Bloodbank instance
# Leave empty to use mock demo mode
VITE_BLOODBANK_WS_URL=ws://localhost:15674/ws

# Bloodbank RabbitMQ Exchange (default: events)
VITE_BLOODBANK_EXCHANGE=events

# Rendering mode: "2d" (PixiJS) or "3d" (PlayCanvas)
VITE_RENDER_MODE=2d
